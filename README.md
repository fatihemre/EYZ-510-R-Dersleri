# 📊 R ile Yapay Zeka ve Eğitimde Veri Analitiği

Bu GitHub deposu, yüksek lisans düzeyindeki **R ile Yapay Zeka ve Eğitimde Veri Analitiği** dersinde haftalık olarak geliştirilen R uygulamalarını içermektedir.

Repo’nun amacı, derste yazılan kodları düzenli ve takip edilebilir bir yapıda sunarak öğrencilerin konuları tekrar edebilmesini ve kendi çalışmalarında referans olarak kullanabilmesini sağlamaktır.

---

## 🎯 Dersin Kapsamı

Bu repo kapsamında ele alınan konular:

**Hafta 01 — R'a Giriş ve Temel Veri Yapıları**
* R sözdizimi, aritmetik işlemler ve değişken tanımlama
* Temel veri tipleri: sayısal, karakter ve mantıksal
* Vektörler: oluşturma, indeksleme ve eleman bazlı işlemler
* Temel istatistiksel fonksiyonlar: `mean()`, `median()`, `table()`, `summary()`
* Data frame oluşturma ve veri seçme (indeksleme)
* Kontrol yapıları: `if / else if / else`
* Faktör kavramı ve `as.factor()` kullanımı

**Hafta 02 — Veri Okuma, Analiz ve Görselleştirme**
* R'ın yerleşik veri setlerini yükleme (`data()`)
* Excel dosyası okuma (`readxl` paketi, `read_excel()`)
* Keşifçi veri analizi: `str()`, `View()`, `summary()`, `table()`
* Merkezi eğilim ve yayılım ölçüleri: ortalama, medyan, standart sapma
* Temel görselleştirme: `barplot()` ve grafik özelleştirme (başlık, eksen etiketleri, renk)
* `dplyr` ile veri manipülasyonu: `mutate()`, `recode()`, `across()` + `where()`
* Pipe operatörü (`%>%`) kullanımı
* Koşula dayalı satır filtreleme

> Not: İçerik, haftalar ilerledikçe güncellenecek ve genişletilecektir.

---

## 📁 Klasör Yapısı

Repo hafta bazlı organize edilmiştir:

```
/hafta_01
/hafta_02
/hafta_03
...
```

Her klasörde:

* Derste yazılan R kodları
* Varsa ilgili veri setleri
* Kod içi açıklamalar

bulunmaktadır.

---

## 💻 Kullanılan Araçlar

* R
* RStudio
* İlgili R paketleri

> Kullanılan paketler, ilgili dosyaların başında belirtilmiştir.

---

## 🚀 Nasıl Kullanılır?

1. Repo’yu klonlayın veya indirin.
2. RStudio ile açın.
3. İlgili haftanın klasörüne giderek kodları sırayla çalıştırın.

---

## 📌 Not

Bu repo bir ders kitabı değil, uygulama arşividir.
Kodları yalnızca çalıştırmak yerine değiştirerek ve deneyerek öğrenmeniz önerilir.
